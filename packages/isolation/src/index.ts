/**
 * `@ald/isolation` — Mode R separate-process / separate-container learner isolation (SPEC §5.2, §10.3; ALD-053/055/056 runtime half, ALD-040 transport half).
 *
 * PLACEHOLDER index: the owning implementation replaces this file.
 */
export const PACKAGE_NAME = '@ald/isolation';
